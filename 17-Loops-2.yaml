- name: INSTALL MULTIPLE  PACKAGES BY USING LOOPS
  hosts: Frontend
  become: yes
  tasks:
  - name: INSTALL 3 PACKAGES 
    ansible.builtin.package:
     name: "{{ item }}"
     state: present
    loop:
     - nginx
     - mysql
     - zip
